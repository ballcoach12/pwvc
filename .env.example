# P-WVC Environment Configuration

# Database connection string
DATABASE_URL=postgres://postgres:password@localhost:5432/pwvc?sslmode=disable

# HTTP server port
PORT=8080

# Gin framework mode (debug, release)
GIN_MODE=debug