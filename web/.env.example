# P-WVC Frontend Environment Configuration

# API URL - Use this to override the default API endpoint
# In development: Uses Vite proxy to /api (no need to set)
# In production: Set to your backend API URL
# VITE_API_URL=https://api.yourpwvcdomain.com/api

# WebSocket URL - Use this to override the default WebSocket endpoint  
# In development: Uses ws://localhost:8080/ws (no need to set)
# In production: Set to your backend WebSocket URL
# VITE_WS_URL=wss://api.yourpwvcdomain.com/ws